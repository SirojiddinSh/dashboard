/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "dummyjson.com",
            "https://cdn.dummyjson.com",
            "cdn.dummyjson.com",
        ],
    },
};

export default nextConfig;
